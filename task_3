using System;

namespace std
{
  class Program
  {
    static void Main(string[] args)
      {
        int number = 0;
        while(number <= 10){
          Console.Write(number);
          Console.Write(" ");
          number = number + 1;
        }
      }
    }
  }
